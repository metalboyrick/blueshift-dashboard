import { ChallengeTitle } from "../../../../components/Challenges/ChallengeTitle";
import { RequirementList } from "../../../../components/Challenges/RequirementList";
import { Requirement } from "../../../../components/Challenges/Requirement";

<ChallengeTitle>Anchor Escrow</ChallengeTitle>

It's now time to put what you've learned to the test. Using what you've learned from the lesson, build an escrow program using the Anchor framework that satisfies the following requirements:

> Your state account should be defined using an account discriminator with a value of `1`.

<RequirementList>
  <Requirement
    title="Challenge 1: Making an escrow offer"
    description="Your program should allow a maker to make an escrow offer. Define an instruction to do this using an instruction discriminator with value `0`."
  />

  <Requirement
    title="Challenge 2: Taking an escrow offer"
    description="Your program should allow a taker to take an escrow offer. Define an instruction to do this using an instruction discriminator value of `1`."
  />

  <Requirement
    title="Challenge 3: Canceling an escrow offer"
    description="Your program should allow the maker to cancel an escrow offer and refund the escrowed funds. Make sure your instruction is defined with an instruction discriminator value of `2`."
  />
</RequirementList>
