# Pinocchio Memo 程式

歡迎來到使用 Pinocchio 的第一個挑戰！您的目標是建立一個簡單的備忘錄程式，將訊息記錄到區塊鏈中。這是了解 Pinocchio 指令如何運作的絕佳入門。

該程式只有一個指令，且沒有任何帳戶。它直接使用 `instruction_data` 作為字串的來源，然後透過 `sol_log` 系統呼叫簡單地輸出該字串。

您的程式還應該具有 `22222222222222222222222222222222222222222222` 的程式 ID 以通過挑戰。