# Pinocchio 备忘录程序

欢迎来到使用 Pinocchio 的第一个挑战！您的目标是创建一个简单的备忘录程序，将消息记录到区块链中。这是了解 Pinocchio 中指令工作原理的一个很好的入门项目。

该程序只有一个指令且没有账户。它直接使用 `instruction_data` 作为字符串的来源，然后通过 `sol_log` 系统调用简单地将该字符串记录下来。

您的程序还应具有 `22222222222222222222222222222222222222222222` 的程序 ID 以通过挑战。