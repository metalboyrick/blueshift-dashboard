# Anchor Memo 程式

歡迎參加使用 Solana 上的 Anchor 框架的第一個挑戰！您的目標是**建立一個簡單的備忘錄程式，將訊息記錄到區塊鏈上**。這是了解 Anchor 中指令如何運作的絕佳入門。

這個程式只有一個指令，並接收：

- 一個 `String` 輸入
- 不需要任何帳戶

然後使用 Anchor 的 `msg!()` 巨集簡單地將這個字串記錄下來。

您的程式還應該有一個程式 ID 為 `22222222222222222222222222222222222222222222` 才能通過挑戰。
