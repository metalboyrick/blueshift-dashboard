# Anchor 备忘录程序

欢迎来到使用 Solana 上的 Anchor 框架的第一个挑战！您的目标是**创建一个简单的备忘录程序，将消息记录到区块链上**。这是了解 Anchor 中指令工作原理的一个很好的入门项目。

该程序只有一个指令，并接收：

- 一个 `String` 输入
- 无账户

然后使用 Anchor 的 `msg!()` 宏简单地将此字符串记录出来。

您的程序还应具有一个程序 ID 为 `22222222222222222222222222222222222222222222`，以通过挑战。
